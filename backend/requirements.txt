# Dependencias propias del API.
# Los modelos y el acceso a datos llegan por el paquete local gym-core,
# que el Dockerfile instala desde ./gym_core.
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-jose[cryptography]==3.3.0
# bcrypt se usa directamente, sin passlib: passlib 1.7.4 depende del modulo
# crypt, que Python elimino en 3.13, y no tiene version que no lo use.
bcrypt==4.2.1
python-multipart==0.0.20
